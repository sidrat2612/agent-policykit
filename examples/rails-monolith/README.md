# Rails Monolith Example

This directory is reserved for a future Rails monolith example.

The current codebase does not yet ship a Rails framework pack, so this example remains a placeholder until the Rails pack is implemented.
